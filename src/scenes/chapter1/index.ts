import Phaser from 'phaser';

/**
 * Chapter 1 (Appalachia) scene bundle.
 * Stream C1 adds scene imports and exports to CHAPTER1_SCENES.
 * Registered in src/config.ts via the aggregator.
 */
export const CHAPTER1_SCENES: Phaser.Types.Scenes.SceneType[] = [];
