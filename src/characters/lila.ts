import type { CharacterDef } from '../types.js';
import { npcStub } from './_npcStub.js';

/** Lila Chen — Dr. Chen's converted daughter. NPC (Ch.5). */
export const LILA_DEF: CharacterDef = npcStub('lila', 'LILA', 0x88aacc, 5);
