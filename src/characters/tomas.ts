import type { CharacterDef } from '../types.js';
import { npcStub } from './_npcStub.js';

/** Tomas Reyes — New Memphis underground. Deja's past. NPC (Ch.2). */
export const TOMAS_DEF: CharacterDef = npcStub('tomas', 'TOMAS', 0x332255, 2);
