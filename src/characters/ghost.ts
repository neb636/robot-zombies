import type { CharacterDef } from '../types.js';
import { npcStub } from './_npcStub.js';

/** Ghost — real pirate radio operator. NPC (Ch.3 Radio Tower). Hands over the encryption key. */
export const GHOST_DEF: CharacterDef = npcStub('ghost', 'GHOST', 0x554488, 3);
