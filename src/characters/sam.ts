import type { CharacterDef } from '../types.js';
import { npcStub } from './_npcStub.js';

/**
 * Sam "Shotgun" Calloway — Appalachia quartermaster. NPC, non-playable.
 * Recurring merchant across Ch.1–3. Stream C1 owns the intro body;
 * Stream C2 / C3 add her dialogue lines when she reappears.
 */
export const SAM_DEF: CharacterDef = npcStub('sam', 'SAM', 0x885522, 1);
