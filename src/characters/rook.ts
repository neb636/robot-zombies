import type { CharacterDef } from '../types.js';
import { npcStub } from './_npcStub.js';

/** Rook — Appalachian trader with rebuilt Sentinel Scout Dog "Three". Cameo Ch.1. */
export const ROOK_DEF: CharacterDef = npcStub('rook', 'ROOK', 0x446655, 1);
