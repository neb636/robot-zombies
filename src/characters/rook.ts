import type { CharacterDef } from '../types.js';
import { npcStub } from './_npcStub.js';

/** Rook — Ridge Camp NPC, scout. Not playable. */
export const ROOK_DEF: CharacterDef = npcStub('rook', 'ROOK', 0x667788, 1);
