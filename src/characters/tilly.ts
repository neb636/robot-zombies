import type { CharacterDef } from '../types.js';
import { npcStub } from './_npcStub.js';

/** Tilly — 11-year-old at Ridge Camp. Mute until Ch.3. NPC. */
export const TILLY_DEF: CharacterDef = npcStub('tilly', 'TILLY', 0xcccc99, 1);
