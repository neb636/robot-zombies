/**
 * Re-exports for dialogue-choice types. Canonical definitions live in
 * src/types.ts — this module exists for convenient single-import by chapter
 * scenes that only need choice-related shapes.
 */
export type { DialogueChoice, DialogueLine } from '../types.js';
