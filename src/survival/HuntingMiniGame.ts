import type { HuntingResult } from '../types.js';

/**
 * Pure logic for the hunting mini-game. Rendering lives in HuntingScene.
 *
 * PHASE B STUB — Stream A fills the arc/press-window math.
 */
export function resolveHunt(_eliasPresent: boolean, _timingMs: number): HuntingResult {
  return 'miss';
}
