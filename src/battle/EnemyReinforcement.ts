import type { ReinforcementSpec } from '../types.js';

/**
 * Spawns a reinforcement enemy during battle. Wired into
 * EnemyAIStateMachine's Sentinel reinforcement-call flow.
 *
 * PHASE B STUB — Stream D fills the BattleManager.spawnReinforcement(...)
 * call path and HUD rebind.
 */
export function spawnReinforcement(
  _manager: unknown,
  _spec: ReinforcementSpec,
): void {
  /* no-op */
}
