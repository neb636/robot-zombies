import type { PassiveEffect } from '../../types.js';
export const MARCUS_PASSIVE: PassiveEffect = {
  characterId: 'marcus',
  description: 'Old Friend: +8 LCK to player while Marcus alive. Removed on conversion.',
};
