import type { PassiveEffect } from '../../types.js';
export const DR_CHEN_PASSIVE: PassiveEffect = {
  characterId: 'dr_chen',
  description: 'Field Engineer: +20% scrap drop rate; vehicle breakdowns auto-resolved.',
};
