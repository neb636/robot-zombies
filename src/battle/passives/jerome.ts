import type { PassiveEffect } from '../../types.js';
export const JEROME_PASSIVE: PassiveEffect = {
  characterId: 'jerome',
  description: 'Anchor: morale never drops below 20 while Jerome is alive.',
};
