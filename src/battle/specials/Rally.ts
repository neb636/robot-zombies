/**
 * Rally — fills a target ally's ATB gauge.
 * PHASE B STUB — Stream D fills the body. Called via src/battle/specials/index.ts.
 */
export function executeRally(_ctx: unknown): void {
  /* no-op */
}
