/**
 * Control / Rewire — Dr. Chen tech. Target enemy attacks its own allies for
 * 2 turns (apply Hacked status; EnemyAIStateMachine already handles enemy-
 * attacking-own when Hacked).
 * PHASE B STUB — Stream D fills.
 */
export function executeControl(_ctx: unknown): void {
  /* no-op */
}
