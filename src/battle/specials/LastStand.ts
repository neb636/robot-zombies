/**
 * Last Stand — passive trigger at HP < 20%; next turn +50% STR for 1 turn.
 * PHASE B STUB — Stream D wires the HP-watch hook. Called from a per-tick
 * check in BattleManager, not from the tech menu.
 */
export function onLastStandCheck(_ctx: unknown): void {
  /* no-op */
}
